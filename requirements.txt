# Dashboard ElectSim España
python-dotenv>=1.0.1
feedparser>=6.0.0
sqlalchemy>=2.0.36
psycopg[binary,pool]>=3.2.6
pandas>=2.2.2
plotly>=5.22.0
streamlit>=1.35.0
requests>=2.31.0
beautifulsoup4>=4.12.3
lxml>=5.2.2
pydantic>=2.7.0
pyyaml>=6.0.2
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
anthropic>=0.25.0
python-multipart>=0.0.9
pyjwt>=2.9.0
polars>=1.8.2
tiktoken>=0.8.0
pgvector>=0.3.6
prefect>=2.19.0,<3.0.0
networkx>=3.3

# ── IA local operativa: RAG, embeddings, NER y sentiment ─────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.2.0
spacy>=3.7.0
sentencepiece>=0.2.0

# ── Microdatos & survey analytics (pyreadstat, weightedcalcs) ────────────────
pyreadstat>=1.2.7            # Leer SPSS/SAV del CIS directamente en pandas
weightedcalcs>=0.1.3         # Cálculos estadísticos con pesos muestrales
scipy>=1.13.0                # Tests estadísticos sobre datos de encuesta

# ── Clustering & ML (kmodes para datos mixtos CIS) ───────────────────────────
scikit-learn>=1.5.0          # KMeans, PCA, preprocessing
kmodes>=0.12.2               # k-modes y k-prototypes para variables categóricas
prince>=0.13.0               # MCA/PCA/FAMD sobre datos mixtos

# ── Modelos estadísticos ──────────────────────────────────────────────────────
statsmodels>=0.14.2          # Regresiones, modelos de respuesta sobre encuestas

# ── ETL & calidad de datos ───────────────────────────────────────────────────
alembic>=1.13.0              # Migraciones DB con autogenerate

# ── Impacto causal (opcional para metodo BSTS) ───────────────────────────────
pycausalimpact>=0.1.1

# ── NLP español — análisis de discurso político ──────────────────────────────
# pysentimiento>=0.7.0         # Sentiment, hate_speech, emotion, NER en español
                               # (descomentar; requiere torch ~1.5GB descarga modelos)
yake>=0.4.8                    # Keyword extraction sin modelos (YAKE)
# Modelo recomendado: python -m spacy download es_core_news_lg

# ── News crawling — medios españoles ─────────────────────────────────────────
# fundus>=0.4.0                # Structured news crawlers: El Pais, El Mundo, ABC, La Vanguardia, etc.
                               # (descomentar si se quiere texto completo vs. RSS)

# ── Bloque 4: Radar Mediatico ────────────────────────────────────────────────
bertopic>=0.16.0               # Topic modeling dinamico (requiere torch)
hdbscan>=0.8.33                # Clustering para BERTopic
umap-learn>=0.5.6              # Reduccion dimensional para BERTopic
# pysentimiento>=0.7.0         # Sentiment/hate/emotion en espanol (torch, 1.5GB)
asyncpg>=0.29.0                # Driver async PostgreSQL para SQLAlchemy

# ── Bloque 5: Ollama + Actor RAG ─────────────────────────────────────────────
# Ollama se ejecuta como servidor local (ollama.com) — sin pip install
# Modelos: qwen3:8b, llama3.2:3b, nomic-embed-text, gemma3:12b
# pgvector ya incluido arriba (pgvector>=0.3.6)

# ── Time series forecasting — proyecciones electorales ───────────────────────
statsforecast>=1.7.0           # AutoARIMA, ETS, Theta, CES — muy rápido

# ── Causal inference — análisis de impacto de eventos ───────────────────────
# causalpy>=0.4.0              # DiD, Synthetic Control, ITS (requiere pymc>=5)
# pymc>=5.0.0                  # Bayesian inference (depende de aesara/pytensor)
# dowhy>=0.11.0                # Microsoft causal graphs

# ── Financial data — correlaciones economía-política ─────────────────────────
# openbb>=4.0.0                # Financial data platform (descomentar para mercados)
# financetoolkit>=1.8.0        # Financial ratios & metrics

# ── Agent frameworks — pipelines IA estructurados ────────────────────────────
pydantic-ai>=0.0.20            # Type-safe AI agents (Pydantic team)
# crewai>=0.30.0               # Multi-agent orchestration
# langchain>=0.2.0             # RAG + LLM chains

# ── Geospatial — mapas España ────────────────────────────────────────────────
# geopandas>=0.14.0            # GeoDataFrames para mapas electorales
# pydeck>=0.8.0                # deck.gl geospatial visualization

# ── Topic modeling — BERTopic ─────────────────────────────────────────────────
# bertopic>=0.16.0             # Dynamic topic modeling (requiere torch)
# sentence-transformers>=2.7.0 # Embeddings para BERTopic

# ── Graph/Network visualization ───────────────────────────────────────────────
pyvis>=0.3.2                   # Interactive network graphs in Streamlit

# ── Geopolítica & RRII — módulo D8 ───────────────────────────────────────────
apscheduler>=3.10.0            # Scheduler para pipeline_geopolitica.py (OSINT/ACLED/briefing)
trafilatura>=1.12.0            # Extracción de texto limpio desde URLs OSINT
# telegram-bot>=13.0           # Alternativa a API REST de Telegram para alertas
telethon>=1.36.0               # MTProto Telegram client (Block 5 social ingestion)
praw>=7.7.0                    # Reddit API wrapper (Block 5 — opcional)

# ── Geopolítica v2 — nuevas dependencias ─────────────────────────────────────
polars>=0.20.0                 # Procesado GKG GDELT (10x más rápido que pandas para CSV masivos)
gdeltdoc>=1.4.0                # GDELT DOC API wrapper (fallback: HTTP directo disponible)
pydeck>=0.8.0                  # deck.gl 3D maps para mapa de riesgo (ColumnLayer + H3)
h3>=3.7.6                      # Indexado hexagonal H3 para GPSJam
wbgapi>=1.0.12                 # World Bank API — WGI governance indicators
# imf-reader>=0.5.0            # IMF WEO data (pip install imf-reader)
# causalpy>=0.4.0              # Modelos causales bayesianos (PyMC-Labs) — opcional

# ── Observabilidad — Bloque 7 ─────────────────────────────────────────────────
opentelemetry-api>=1.24.0                         # API OTel (tracers, meters, context)
opentelemetry-sdk>=1.24.0                         # SDK OTel con TracerProvider + MeterProvider
opentelemetry-exporter-otlp-proto-grpc>=1.24.0    # Exportador OTLP gRPC → OTel Collector
opentelemetry-instrumentation-fastapi>=0.45b0     # Auto-instrumentacion FastAPI (HTTP spans)
opentelemetry-instrumentation-sqlalchemy>=0.45b0  # Auto-instrumentacion SQLAlchemy (DB spans)
opentelemetry-instrumentation-httpx>=0.45b0       # Auto-instrumentacion httpx (LiteLLM calls)

# ── Sprint 3 Briefings PDF export ────────────────────────────────────────────
reportlab>=4.0
