name: Deploy Dev

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build images
        run: docker compose build electsim-api electsim-dashboard prefect-worker
      - name: Push step placeholder
        run: echo "Configurar registry y credenciales del entorno dev"
      - name: Remote deploy placeholder
        run: echo "docker compose pull && docker compose up -d en servidor dev"
