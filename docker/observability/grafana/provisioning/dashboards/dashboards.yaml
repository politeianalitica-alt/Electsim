apiVersion: 1

providers:
  - name: ElectSim
    orgId: 1
    folder: ElectSim
    folderUid: electsim
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
